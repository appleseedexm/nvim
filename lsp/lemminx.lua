return {
    cmd = { "lemminx" },
    root_markers = {},
    filetypes = { 'xml' },
    settings = {
        xml = {
            server = {
                workDir = "~/.cache/lemminx",
            }
        }
    }
}
